* Modify folder / file path as needed.
  Example: create /js folder and put all .js files there.
  During build process (via webpack), all .js files will go in /js folder. Make sure file names are unique.
  